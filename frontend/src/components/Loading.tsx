const Loading = () => {
  return (
    <main className="flex min-h-screen items-center justify-center bg-background">
      <div className="h-40 w-40 animate-spin rounded-full border-t-2 border-primary" />
    </main>
  );
};

export default Loading;
