const Loading = () => {
  return (
    <div className="flex min-h-screen items-center justify-center bg-primary">
      <div className="h-40 w-40 animate-spin rounded-full border-t-2 border-primary-foreground" />
    </div>
  );
};

export default Loading;
